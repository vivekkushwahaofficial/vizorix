# ==============================================================================
# DEVELOPMENTS PROFILE - VIZORIX API
# ==============================================================================

# Standard database variables mapped to development environment instances
DB_URL: ${DB_URL}
DB_USERNAME: ${DB_USERNAME}
DB_PASSWORD: ${DB_PASSWORD}

# Development logging levels
logging:
  level:
    root: INFO
    com.vizorix.api: DEBUG
    org.flywaydb: INFO
